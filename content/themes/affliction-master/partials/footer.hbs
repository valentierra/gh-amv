<footer class="affliction-footer">
    <section class="copyright">
        &copy; {{date format="YYYY"}} <a href="{{@site.url}}">{{@site.title}}</a>  
    </section>

    <div text-align=left>
        <a>Home Builders Association Member</a>
    </div>

    <div class="social-icons">
        <div class="site-nav-left">
            <a href="https://amvconstruction.com/contact/">Contact Us</a>
            <!-- Uncomment to activate social networks icons
            <nav class="site-footer-nav">
                {{#if @site.facebook}}
                    <a class="social" href="{{facebook_url @site.facebook}}" target="_blank" rel="noopener">{{> "icons/facebook"}}</a>{{/if}}
                {{#if @site.twitter}}
                    <a class="social" href="{{twitter_url @site.twitter}}" target="_blank" rel="noopener">{{> "icons/twitter"}}</a>{{/if}}
            </nav>
            -->
        </div>
    </div>
</footer>